# FIELDNOTE

A warm editorial blog built with TanStack Start and TanStack Router. Posts and
comment threads are intentionally backed by local JSON fixtures for now.

## Local development

```bash
npm install
npm run dev
```

The homepage lives at `/`, with articles at `/blog/:slug`. The article comment
feed supports local composing, replies, likes, and top/latest sorting without a
backend.
